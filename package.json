{
  "name": "quote-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "server": "cd server && npm start && cd ..",
    "client": "cd client && npm start && cd ..",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\""
  },
  "dependencies": {},
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jsaindon/quote-app.git"
  },
  "homepage": "https://github.com/jsaindon/quote-app#readme",
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
